# Render blueprint for the Atlas Bank API (the web app is deployed on Vercel).
# Secrets (JWT_SECRET, ENCRYPTION_KEY, SWAN_*, RESEND_API_KEY, SENTRY_DSN) are
# set in the Render dashboard / env groups, never committed.
services:
  - type: web
    name: atlas-api
    runtime: docker
    dockerfilePath: ./apps/api/Dockerfile
    dockerContext: .
    healthCheckPath: /api/v1/health
    plan: standard
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
      - key: DATABASE_URL
        fromDatabase:
          name: atlas-postgres
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: atlas-redis
          type: redis
          property: connectionString
      - fromGroup: atlas-secrets

  - type: redis
    name: atlas-redis
    plan: standard
    ipAllowList: []

databases:
  - name: atlas-postgres
    plan: standard
    postgresMajorVersion: "15"
